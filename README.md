# 📝 Sarvam Voice Notes
Record voice notes in Indian languages → auto-transcribe → translate → searchable.

```bash
pip install -r requirements.txt && cp .env.example .env
python notes.py
# Commands: [r]ecord  [s]earch  [l]ist  [q]uit
```
